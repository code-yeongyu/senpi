# Debug Journal — provider session recovery hardening

Started: 2026-05-15T00:00:00+09:00
Goal: Harden provider/session recovery for malformed tool-call histories, preferring extension-layer fixes.

## Environment snapshot

- Runtime: Node.js v26.0.0, npm 11.12.1
- Entry: package tests via `npx tsx ../../node_modules/vitest/dist/cli.js --run ...`
- Git worktree: `/Users/yeongyu/local-workspaces/senpi-provider-recovery-ulw`
- References read: debugging Node runtime, setup, investigation, fix, QA, cleanup references

## Hypotheses

1. [CONFIRMED BY CODE] Chat Completions-compatible providers can still receive orphan `role: "tool"` messages because `tool-pair-guard` only covers Anthropic and OpenAI Responses payloads. Evidence: `tool-pair-guard/index.ts` runs only `sanitizeAnthropicPayload()` and `sanitizeOpenAIResponsesPayload()`.
2. [DEFERRED] Google `functionResponse` orphan payloads may have an equivalent provider-specific gap. Evidence: `google-shared.ts` converts every `toolResult` into a `functionResponse`; no guard exists yet.
3. [DEFERRED] Shared `repairOrphanedToolResults()` keeps orphan messages as `toolResult`, so standalone `pi-ai` users may still hit provider validation errors. Evidence: `tool-pair-repair.ts` replaces content but preserves role.

## Artifacts to revert

- [ ] tmux session `provider-recovery-qa-server`. Kill: `tmux kill-session -t provider-recovery-qa-server`
- [ ] `/tmp/provider-recovery-qa-server.mjs`. Remove: `rm -f /tmp/provider-recovery-qa-server.mjs`
- [ ] `/tmp/provider-recovery-qa-driver.mjs`. Remove: `rm -f /tmp/provider-recovery-qa-driver.mjs`

## Findings

Research is recorded in `docs/provider-recovery-research.md` and plan in `docs/provider-recovery-goal-plan.md`.
