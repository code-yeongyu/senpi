/**
 * Wire identity sent to the provider and persisted in user data.
 * Frozen at "claude-sdk-oauth" permanently — must NOT follow a provider-id rename.
 * `packages/ai/src/utils/prompt-cache-ttl.ts:358` and the compaction lane policy switch on this exact string.
 */
export const CLAUDE_SDK_OAUTH_API_ID = "claude-sdk-oauth";
